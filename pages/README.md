Images are called from webfiles\img.png

QR codes are called from webfiles\qr.png

all text placeholders are called as "replace<>" with the field name in <>. These include courseNameText, locationText, headText and bodyText fields. Not all used all the time. 
